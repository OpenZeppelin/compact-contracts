{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "compact": {
      "env": ["COMPACT_HOME"],
      "inputs": ["src/**/*.compact"],
      "outputLogs": "new-only",
      "outputs": ["src/artifacts/**", "src/gen/**", "gen/**"]
    },
    "test": {
      "outputs": [],
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "env": ["COMPACT_HOME"],
      "inputs": [
        "src/**/*.ts",
        "!src/**/*.test.ts",
        "!tests/**/*.ts",
        "tsconfig.json",
        "tsconfig.build.json",
        ".env"
      ],
      "outputs": ["dist/**"]
    },
    "types": {
      "outputs": [],
      "cache": false,
      "dependsOn": ["compact"]
    },
    "//#fmt-and-lint": {},
    "//#fmt-and-lint:ci": {},
    "//#fmt-and-lint:fix": {
      "cache": false
    },
    "clean": {
      "outputs": [],
      "cache": false
    },
    "docs": {
      "outputs": [],
      "cache": false
    },
    "docs:watch": {
      "outputs": [],
      "cache": false
    }
  }
}
